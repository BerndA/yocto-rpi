language: bash

sudo: false

git: 
        - depth 3

addons:
  apt:
    packages:
    - bash 
    - gawk 
    - wget 
    - git-core 
    - diffstat 
    - unzip 
    - texinfo 
    - gcc-multilib
    - build-essential
    - chrpath 
    - socat 
    - cpio 
    - python 
    - python3 
    - python3-pip 
    - python-pexpect 
    - locale  
    
install: 
       - echo 1
    
script:
      - /bin/bash -c 'source yocto/oe-init-build-env ./work && bitbake core-image-minimal'
